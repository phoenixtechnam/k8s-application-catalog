apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "bookstack.fullname" . }}-bookstack
  labels:
    {{- include "bookstack.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "bookstack.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: bookstack
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 80
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "bookstack.fullname" . }}-mariadb
  labels:
    {{- include "bookstack.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "bookstack.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mariadb
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "bookstack.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: bookstack
      ports:
        - port: 3306
          protocol: TCP
