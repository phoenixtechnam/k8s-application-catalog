apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "prestashop.fullname" . }}-prestashop
  labels:
    {{- include "prestashop.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "prestashop.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: prestashop
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 80
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "prestashop.fullname" . }}-mariadb
  labels:
    {{- include "prestashop.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "prestashop.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mariadb
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "prestashop.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: prestashop
      ports:
        - port: 3306
          protocol: TCP
