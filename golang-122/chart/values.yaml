replicaCount: 1

image:
  repository: ghcr.io/phoenixtechnam/k8s-application-catalog/golang-122
  tag: "1.22"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: nginx
  domain: ""
  tls: true

resources:
  requests:
    cpu: 0.25
    memory: 256Mi
  limits:
    cpu: 0.25
    memory: 256Mi

persistence:
  enabled: true
  mountPath: "/app"
  size: 5Gi

env: {}
